<view class="jardinage-container">
  <view class="mobile-card">

    <!-- Standardized Header -->
    <view class="horaire-header-wrap">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--horaire">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble" bindtap="onProfileTap">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/profile.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>
    </view>

    <!-- Hero Section -->
    <view class="jardinage-hero-area">
      <view class="jardinage-hero-main">
        <image class="jardinage-hero-img" src="/assets/club.png" mode="aspectFill"></image>
        <view class="jardinage-hero-overlay">
          <text class="jardinage-hero-title">DÉCOUVREZ NOS CLUBS TIQQA</text>
          <view class="jardinage-hero-btn" bindtap="onHeroCta">
            <text class="jardinage-hero-btn-txt">ACCÉDER</text>
          </view>
        </view>
      </view>
    </view>

    <!-- Date Navigation Strip -->
    <view class="jardinage-date-strip">
      <view class="date-arrow-btn" bindtap="onPrevDate">←</view>
      <view class="date-center">
        <image class="date-calendar-icon" src="/assets/vocale.png" mode="aspectFit"></image>
        <text class="date-text">{{currentDate}}</text>
      </view>
      <view class="date-arrow-btn" bindtap="onNextDate">→</view>
    </view>

    <!-- Main Title -->
    <view class="jardinage-main-header">
      <view class="jardinage-title-row">
        <image class="jardinage-title-img-script" src="/assets/Clubs.png" mode="heightFix"></image>
        <text class="jardinage-title-text">JARDINAGE</text>
      </view>
    </view>

    <!-- Scrollable Cards & Scrollbar Area -->
    <view class="jardinage-grid-container">

      <scroll-view class="content-scroll" scroll-y bindscroll="onScroll" show-scrollbar="{{false}}" enhanced="{{true}}">
        <view class="jardinage-grid">

          <!-- Activités Card -->
          <view class="jardinage-card" bindtap="onCardTap" data-type="activites">
            <view class="jardinage-card-img-wrap">
              <image class="jardinage-card-img" src="/assets/J1.png" mode="aspectFill"></image>
            </view>
            <view class="jardinage-card-footer">
              <text class="jardinage-card-label">ACTIVITÉS</text>
            </view>
          </view>

          <!-- Communauté Card -->
          <view class="jardinage-card" bindtap="onCardTap" data-type="communaute">
            <view class="jardinage-card-img-wrap">
              <image class="jardinage-card-img" src="/assets/J2.png" mode="aspectFill"></image>
            </view>
            <view class="jardinage-card-footer">
              <text class="jardinage-card-label">COMMUNAUTÉ</text>
            </view>
          </view>

          <!-- Vidéos Card -->
          <view class="jardinage-card" bindtap="onCardTap" data-type="videos">
            <view class="jardinage-card-img-wrap">
              <image class="jardinage-card-img" src="/assets/J3.png" mode="aspectFill"></image>
            </view>
            <view class="jardinage-card-footer">
              <text class="jardinage-card-label">VIDÉOS</text>
            </view>
          </view>

          <!-- Coach Card -->
          <view class="jardinage-card" bindtap="onCardTap" data-type="coach">
            <view class="jardinage-card-img-wrap">
              <image class="jardinage-card-img" src="/assets/J4.png" mode="aspectFill"></image>
            </view>
            <view class="jardinage-card-footer">
              <text class="jardinage-card-label">COACH</text>
            </view>
          </view>

        </view>
        <view class="bottom-spacer"></view>
      </scroll-view>

      <!-- Dynamic Functional Scrollbar -->
      <view class="jardinage-scrollbar">
        <view class="jardinage-scroll-arrow">↑</view>
        <view class="jardinage-scroll-track">
          <view class="jardinage-scroll-thumb" style="top: calc({{scrollPercent}}% - {{scrollPercent * 0.8}}rpx);"></view>
        </view>
        <view class="jardinage-scroll-arrow">↓</view>
      </view>

    </view>

    <!-- Bottom Navigation Bar -->
    <bottom-nav-bar variant="fixed" message-count="6" bind:navtap="onNavTap" />
  </view>
</view>
