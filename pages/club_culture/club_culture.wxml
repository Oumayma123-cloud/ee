<view class="culture-container">
  <view class="mobile-card">

    <!-- Standardized Header -->
    <view class="horaire-header-wrap">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--horaire">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble" bindtap="onProfileTap">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/profile.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>
    </view>

    <!-- Hero Section -->
    <view class="culture-hero-area">
      <view class="culture-hero-main">
        <image class="culture-hero-img" src="/assets/club.png" mode="aspectFill"></image>
        <view class="culture-hero-overlay">
          <text class="culture-hero-title">DÉCOUVREZ NOS CLUBS TIQQA</text>
          <view class="culture-hero-btn" bindtap="onHeroCta">
            <text class="culture-hero-btn-txt">ACCÉDER</text>
          </view>
        </view>
      </view>
    </view>

    <!-- Date Navigation Strip -->
    <view class="culture-date-strip">
      <view class="date-arrow-btn" bindtap="onPrevDate">←</view>
      <view class="date-center">
        <image class="date-calendar-icon" src="/assets/vocale.png" mode="aspectFit"></image>
        <text class="date-text">{{currentDate}}</text>
      </view>
      <view class="date-arrow-btn" bindtap="onNextDate">→</view>
    </view>

    <!-- Main Title -->
    <view class="culture-main-header">
      <view class="culture-title-row">
        <image class="culture-title-img-script" src="/assets/Clubs.png" mode="heightFix"></image>
        <text class="culture-title-text">CULTURE</text>
      </view>
    </view>

    <!-- Scrollable Cards & Scrollbar Area -->
    <view class="culture-grid-container">

      <scroll-view class="content-scroll" scroll-y bindscroll="onScroll" show-scrollbar="{{false}}" enhanced="{{true}}">
        <view class="culture-grid">

          <!-- Activités Card -->
          <view class="culture-card" bindtap="onCardTap" data-type="activites">
            <view class="culture-card-img-wrap">
              <image class="culture-card-img" src="/assets/CU1.png" mode="aspectFill"></image>
            </view>
            <view class="culture-card-footer">
              <text class="culture-card-label">ACTIVITÉS</text>
            </view>
          </view>

          <!-- Communauté Card -->
          <view class="culture-card" bindtap="onCardTap" data-type="communaute">
            <view class="culture-card-img-wrap">
              <image class="culture-card-img" src="/assets/CU2.png" mode="aspectFill"></image>
            </view>
            <view class="culture-card-footer">
              <text class="culture-card-label">COMMUNAUTÉ</text>
            </view>
          </view>

          <!-- Vidéos Card -->
          <view class="culture-card" bindtap="onCardTap" data-type="videos">
            <view class="culture-card-img-wrap">
              <image class="culture-card-img" src="/assets/CU3.png" mode="aspectFill"></image>
            </view>
            <view class="culture-card-footer">
              <text class="culture-card-label">VIDÉOS</text>
            </view>
          </view>

          <!-- Coach Card -->
          <view class="culture-card" bindtap="onCardTap" data-type="coach">
            <view class="culture-card-img-wrap">
              <image class="culture-card-img" src="/assets/CU4.png" mode="aspectFill"></image>
            </view>
            <view class="culture-card-footer">
              <text class="culture-card-label">COACH</text>
            </view>
          </view>

        </view>
        <view class="bottom-spacer"></view>
      </scroll-view>

      <!-- Dynamic Functional Scrollbar -->
      <view class="culture-scrollbar">
        <view class="culture-scroll-arrow">↑</view>
        <view class="culture-scroll-track">
          <view class="culture-scroll-thumb" style="top: calc({{scrollPercent}}% - {{scrollPercent * 0.8}}rpx);"></view>
        </view>
        <view class="culture-scroll-arrow">↓</view>
      </view>

    </view>

    <!-- Bottom Navigation Bar -->
    <bottom-nav-bar variant="fixed" message-count="6" bind:navtap="onNavTap" />
  </view>
</view>
