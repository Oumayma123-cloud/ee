<view class="bienetre-container">
  <view class="mobile-card">

    <!-- Standardized Header -->
    <view class="horaire-header-wrap">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--horaire">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble" bindtap="onProfileTap">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/profile.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>
    </view>

    <!-- Hero Section -->
    <view class="bienetre-hero-area">
      <view class="bienetre-hero-main">
        <image class="bienetre-hero-img" src="/assets/club.png" mode="aspectFill"></image>
        <view class="bienetre-hero-overlay">
          <text class="bienetre-hero-title">TISSEZ DES LIENS DE CONFIANCE</text>
          <view class="bienetre-hero-btn" bindtap="onHeroCta">
            <text class="bienetre-hero-btn-txt">ACCÉDER</text>
          </view>
        </view>
      </view>
    </view>

    <!-- Date Navigation Strip -->
    <view class="bienetre-date-strip">
      <view class="date-arrow-btn" bindtap="onPrevDate">←</view>
      <view class="date-center">
        <image class="date-calendar-icon" src="/assets/vocale.png" mode="aspectFit"></image>
        <text class="date-text">{{currentDate}}</text>
      </view>
      <view class="date-arrow-btn" bindtap="onNextDate">→</view>
    </view>

    <!-- Main Title -->
    <view class="bienetre-main-header">
      <view class="bienetre-title-row">
        <image class="bienetre-title-img-mes" src="/assets/bien9.png" mode="heightFix"></image>
        <image class="bienetre-title-img-bienetre" src="/assets/bien10.png" mode="heightFix"></image>
      </view>
    </view>

    <!-- Scrollable Cards & Scrollbar Area -->
    <view class="bienetre-grid-container">

      <scroll-view class="content-scroll" scroll-y bindscroll="onScroll" show-scrollbar="{{false}}" enhanced="{{true}}">
        <view class="bienetre-grid">

          <!-- Activités Card -->
          <view class="bienetre-card" bindtap="onCardTap" data-type="activites">
            <view class="bienetre-card-img-wrap">
              <image class="bienetre-card-img" src="/assets/bien1.png" mode="aspectFit"></image>
            </view>
            <view class="bienetre-card-footer">
              <text class="bienetre-card-label">ACTIVITÉS</text>
            </view>
          </view>

          <!-- Communauté Card -->
          <view class="bienetre-card" bindtap="onCardTap" data-type="communaute">
            <view class="bienetre-card-img-wrap">
              <image class="bienetre-card-img" src="/assets/bien2.png" mode="aspectFit"></image>
            </view>
            <view class="bienetre-card-footer">
              <text class="bienetre-card-label">COMMUNAUTÉ</text>
            </view>
          </view>

          <!-- Vidéos Card -->
          <view class="bienetre-card" bindtap="onCardTap" data-type="videos">
            <view class="bienetre-card-img-wrap">
              <image class="bienetre-card-img" src="/assets/bien3.png" mode="aspectFit"></image>
            </view>
            <view class="bienetre-card-footer">
              <text class="bienetre-card-label">VIDÉOS</text>
            </view>
          </view>

          <!-- Coach Card -->
          <view class="bienetre-card" bindtap="onCardTap" data-type="coach">
            <view class="bienetre-card-img-wrap">
              <image class="bienetre-card-img" src="/assets/bien4.png" mode="aspectFit"></image>
            </view>
            <view class="bienetre-card-footer">
              <text class="bienetre-card-label">COACH</text>
            </view>
          </view>

        </view>
        <view class="bottom-spacer"></view>
      </scroll-view>

      <!-- Dynamic Functional Scrollbar -->
      <view class="bienetre-scrollbar">
        <view class="bienetre-scroll-arrow">↑</view>
        <view class="bienetre-scroll-track">
          <view class="bienetre-scroll-thumb" style="top: calc({{scrollPercent}}% - {{scrollPercent * 0.8}}rpx);"></view>
        </view>
        <view class="bienetre-scroll-arrow">↓</view>
      </view>

    </view>

    <!-- Bottom Navigation Bar -->
    <bottom-nav-bar variant="fixed" message-count="6" bind:navtap="onNavTap" />
  </view>
</view>
