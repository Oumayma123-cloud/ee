<view class="technologie-container">
  <view class="mobile-card">

    <!-- Standardized Header -->
    <view class="horaire-header-wrap">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--horaire">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble" bindtap="onProfileTap">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/profile.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>
    </view>

    <!-- Hero Section -->
    <view class="technologie-hero-area">
      <view class="technologie-hero-main">
        <image class="technologie-hero-img" src="/assets/club.png" mode="aspectFill"></image>
        <view class="technologie-hero-overlay">
          <text class="technologie-hero-title">TISSEZ DES LIENS DE CONFIANCE</text>
          <view class="technologie-hero-btn" bindtap="onHeroCta">
            <text class="technologie-hero-btn-txt">ACCÉDER</text>
          </view>
        </view>
      </view>
    </view>

    <!-- Date Navigation Strip -->
    <view class="technologie-date-strip">
      <view class="date-arrow-btn" bindtap="onPrevDate">←</view>
      <view class="date-center">
        <image class="date-calendar-icon" src="/assets/vocale.png" mode="aspectFit"></image>
        <text class="date-text">{{currentDate}}</text>
      </view>
      <view class="date-arrow-btn" bindtap="onNextDate">→</view>
    </view>

    <!-- Main Title -->
    <view class="technologie-main-header">
      <view class="technologie-title-row">
        <image class="technologie-title-img-script" src="/assets/Clubs.png" mode="heightFix"></image>
        <image class="technologie-title-img-technologie" src="/assets/T.png" mode="heightFix"></image>
      </view>
    </view>

    <!-- Scrollable Cards & Scrollbar Area -->
    <view class="technologie-grid-container">

      <scroll-view class="content-scroll" scroll-y bindscroll="onScroll" show-scrollbar="{{false}}" enhanced="{{true}}">
        <view class="technologie-grid">

          <!-- Activités Card -->
          <view class="technologie-card" bindtap="onCardTap" data-type="activites">
            <view class="technologie-card-img-wrap">
              <image class="technologie-card-img" src="/assets/T1.png" mode="aspectFill"></image>
            </view>
            <view class="technologie-card-footer">
              <text class="technologie-card-label">ACTIVITÉS</text>
            </view>
          </view>

          <!-- Communauté Card -->
          <view class="technologie-card" bindtap="onCardTap" data-type="communaute">
            <view class="technologie-card-img-wrap">
              <image class="technologie-card-img" src="/assets/T2.png" mode="aspectFill"></image>
            </view>
            <view class="technologie-card-footer">
              <text class="technologie-card-label">COMMUNAUTÉ</text>
            </view>
          </view>

          <!-- Vidéos Card -->
          <view class="technologie-card" bindtap="onCardTap" data-type="videos">
            <view class="technologie-card-img-wrap">
              <image class="technologie-card-img" src="/assets/T3.png" mode="aspectFill"></image>
            </view>
            <view class="technologie-card-footer">
              <text class="technologie-card-label">VIDÉOS</text>
            </view>
          </view>

          <!-- Coach Card -->
          <view class="technologie-card" bindtap="onCardTap" data-type="coach">
            <view class="technologie-card-img-wrap">
              <image class="technologie-card-img" src="/assets/T4.png" mode="aspectFill"></image>
            </view>
            <view class="technologie-card-footer">
              <text class="technologie-card-label">COACH</text>
            </view>
          </view>

        </view>
        <view class="bottom-spacer"></view>
      </scroll-view>

      <!-- Dynamic Functional Scrollbar -->
      <view class="technologie-scrollbar">
        <view class="technologie-scroll-arrow">↑</view>
        <view class="technologie-scroll-track">
          <view class="technologie-scroll-thumb" style="top: calc({{scrollPercent}}% - {{scrollPercent * 0.8}}rpx);"></view>
        </view>
        <view class="technologie-scroll-arrow">↓</view>
      </view>

    </view>

    <!-- Bottom Navigation Bar -->
    <bottom-nav-bar variant="fixed" message-count="6" bind:navtap="onNavTap" />
  </view>
</view>
