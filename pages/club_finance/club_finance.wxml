<view class="finance-container">
  <view class="mobile-card">

    <!-- Standardized Header -->
    <view class="horaire-header-wrap">
      <view class="status-row">
        <device-status-bar fillStatusBar="{{true}}" edgePadding="{{false}}" />
      </view>
      <view class="top-bar top-bar--horaire">
        <view class="back-wrap" bindtap="onBack">
          <view class="back-circle">←</view>
          <text class="back-text">Retour</text>
        </view>
        <image class="logo" src="/assets/logo/logo-tiqqa.png" mode="aspectFit"></image>
        <view class="avatar-bubble" bindtap="onProfileTap">
          <view class="avatar-wrap avatar-wrap--bubble">
            <image class="avatar" src="/assets/profile.png" mode="aspectFill"></image>
          </view>
          <view class="avatar-bubble-tail"></view>
        </view>
      </view>
    </view>

    <!-- Hero Section -->
    <view class="finance-hero-area">
      <view class="finance-hero-main">
        <image class="finance-hero-img" src="/assets/club.png" mode="aspectFill"></image>
        <view class="finance-hero-overlay">
          <text class="finance-hero-title">DÉCOUVREZ NOS CLUBS TIQQA</text>
          <view class="finance-hero-btn" bindtap="onHeroCta">
            <text class="finance-hero-btn-txt">ACCÉDER</text>
          </view>
        </view>
      </view>
    </view>

    <!-- Date Navigation Strip -->
    <view class="finance-date-strip">
      <view class="date-arrow-btn" bindtap="onPrevDate">←</view>
      <view class="date-center">
        <image class="date-calendar-icon" src="/assets/vocale.png" mode="aspectFit"></image>
        <text class="date-text">{{currentDate}}</text>
      </view>
      <view class="date-arrow-btn" bindtap="onNextDate">→</view>
    </view>

    <!-- Main Title -->
    <view class="finance-main-header">
      <view class="finance-title-row">
        <image class="finance-title-img-script" src="/assets/Clubs.png" mode="heightFix"></image>
        <text class="finance-title-text">FINANCE</text>
      </view>
    </view>

    <!-- Scrollable Cards & Scrollbar Area -->
    <view class="finance-grid-container">

      <scroll-view class="content-scroll" scroll-y bindscroll="onScroll" show-scrollbar="{{false}}" enhanced="{{true}}">
        <view class="finance-grid">

          <!-- Activités Card -->
          <view class="finance-card" bindtap="onCardTap" data-type="activites">
            <view class="finance-card-img-wrap">
              <image class="finance-card-img" src="/assets/F1.png" mode="aspectFill"></image>
            </view>
            <view class="finance-card-footer">
              <text class="finance-card-label">ACTIVITÉS</text>
            </view>
          </view>

          <!-- Communauté Card -->
          <view class="finance-card" bindtap="onCardTap" data-type="communaute">
            <view class="finance-card-img-wrap">
              <image class="finance-card-img" src="/assets/F2.png" mode="aspectFill"></image>
            </view>
            <view class="finance-card-footer">
              <text class="finance-card-label">COMMUNAUTÉ</text>
            </view>
          </view>

          <!-- Vidéos Card -->
          <view class="finance-card" bindtap="onCardTap" data-type="videos">
            <view class="finance-card-img-wrap">
              <image class="finance-card-img" src="/assets/F3.png" mode="aspectFill"></image>
            </view>
            <view class="finance-card-footer">
              <text class="finance-card-label">VIDÉOS</text>
            </view>
          </view>

          <!-- Coach Card -->
          <view class="finance-card" bindtap="onCardTap" data-type="coach">
            <view class="finance-card-img-wrap">
              <image class="finance-card-img" src="/assets/F4.png" mode="aspectFill"></image>
            </view>
            <view class="finance-card-footer">
              <text class="finance-card-label">COACH</text>
            </view>
          </view>

        </view>
        <view class="bottom-spacer"></view>
      </scroll-view>

      <!-- Dynamic Functional Scrollbar -->
      <view class="finance-scrollbar">
        <view class="finance-scroll-arrow">↑</view>
        <view class="finance-scroll-track">
          <view class="finance-scroll-thumb" style="top: calc({{scrollPercent}}% - {{scrollPercent * 0.8}}rpx);"></view>
        </view>
        <view class="finance-scroll-arrow">↓</view>
      </view>

    </view>

    <!-- Bottom Navigation Bar -->
    <bottom-nav-bar variant="fixed" message-count="6" bind:navtap="onNavTap" />
  </view>
</view>
